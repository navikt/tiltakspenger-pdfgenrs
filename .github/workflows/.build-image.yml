name: Build docker image

on:
  workflow_call:
    inputs:
      DOCKERFILE:
        description: "Dockerfile som bygges. Konteksten er alltid repo-rota."
        required: false
        type: string
        default: Dockerfile
      IMAGE_SUFFIX:
        description: "Suffiks på imagenavnet, slik at flere image fra samme repo ikke overskriver hverandre (f.eks. «demo»)."
        required: false
        type: string
        default: ""
    outputs:
      IMAGE:
        description: "Docker image"
        value: ${{ jobs.build.outputs.IMAGE }}

jobs:
  build:
    name: Build docker image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    outputs:
      IMAGE: ${{ steps.docker-push.outputs.image }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Build and push docker image
        # Versjonsoversikt (tag -> SHA): https://github.com/nais/docker-build-push/tags
        # Denne SHA-en: https://github.com/nais/docker-build-push/commit/8e41f1f58749b9248e376c25d33bfbdd2b1163c0
        uses: nais/docker-build-push@8e41f1f58749b9248e376c25d33bfbdd2b1163c0 # v0
        id: docker-push
        with:
          team: tpts
          dockerfile: ${{ inputs.DOCKERFILE }}
          image_suffix: ${{ inputs.IMAGE_SUFFIX }}
